# 🚀 Phase 3 快速開始指南

## 立即測試 AI 功能

### 第一步：啟動後端（5 分鐘）

```bash
# 1. 進入後端目錄
cd /workspaces/vibe-commerce/backend

# 2. 取得 Gemini API Key
# 訪問: https://aistudio.google.com/app/apikey
# 免費申請 API Key

# 3. 設定環境變數
# 編輯 .dev.vars 文件
nano .dev.vars

# 將以下行改為你的實際 API Key:
# GEMINI_API_KEY=your_actual_api_key_here

# 4. 執行資料庫 migration（如果還沒執行）
npx wrangler d1 execute vibe-commerce-db --local \
  --file=./src/db/migrations/0000_initial.sql

npx wrangler d1 execute vibe-commerce-db --local \
  --file=./migrations/0002_add_low_stock_threshold.sql

# 5. 檢查商品狀態
npx tsx scripts/check-products.ts

# 6. 為商品生成向量（必須！）
export $(cat .dev.vars | xargs) && npx tsx scripts/vectorize-products.ts

# 7. 啟動後端服務
npm run dev
```

**預期輸出**:
```
⛅️ wrangler 3.96.0
-------------------
wrangler dev now uses local mode by default, powered by 🔥 Miniflare...
Ready on http://localhost:8787
```

---

### 第二步：啟動前端（2 分鐘）

```bash
# 1. 開啟新終端，進入前端目錄
cd /workspaces/vibe-commerce/frontend

# 2. 安裝依賴（如果還沒安裝）
npm install

# 3. 啟動前端服務
npm run dev
```

**預期輸出**:
```
▲ Next.js 16.1.1
- Local:        http://localhost:3000
```

---

### 第三步：測試 AI 功能（10 分鐘）

#### 1. 訪問 AI Demo 頁面
```
http://localhost:3000/ai-demo
```

#### 2. 測試語義搜尋
在搜尋框輸入：
- ✨ "wireless headphones"
- ✨ "fitness tracker"
- ✨ "comfortable clothing"

**預期效果**:
- 下拉選單顯示搜尋結果
- 顯示相似度評分
- 點擊可進入商品頁面

#### 3. 測試 AI 聊天助手
1. 點擊右下角的紫色聊天按鈕
2. 輸入以下問題測試：
   - 💬 "What are your best-selling products?"
   - 💬 "I need a gift for my friend who loves tech"
   - 💬 "Show me affordable headphones"

**預期效果**:
- 聊天視窗彈出
- AI 回應即時串流顯示
- 文字帶有打字效果

#### 4. 測試個人化推薦
訪問 Dashboard:
```
http://localhost:3000/dashboard
```

**預期效果**:
- 顯示「Recommended for You」區塊
- 商品卡片交錯淡入動畫
- 標記為「AI Pick」

---

## 🎯 功能檢查清單

完成以下檢查以確保所有功能正常：

- [ ] ✅ 後端運行在 http://localhost:8787
- [ ] ✅ 前端運行在 http://localhost:3000
- [ ] ✅ 可以訪問 `/health` 端點
- [ ] ✅ 商品已經向量化（3個商品）
- [ ] ✅ 可以登入 Clerk 帳號
- [ ] ✅ 語義搜尋返回結果
- [ ] ✅ AI 聊天按鈕可見
- [ ] ✅ AI 回應正常串流
- [ ] ✅ 推薦商品正常顯示

---

## 📸 預期效果截圖

### AI Demo 頁面
```
┌─────────────────────────────────────────────┐
│  AI-Powered Shopping Experience             │
│  Discover our intelligent features          │
├─────────────────────────────────────────────┤
│  🔍 Semantic Search                         │
│  ┌──────────────────────────────────────┐   │
│  │ [Search icon] Try: 'wireless...'     │   │
│  └──────────────────────────────────────┘   │
├─────────────────────────────────────────────┤
│  ✨ Personalized Recommendations           │
│  [產品卡片網格，帶有動畫]                  │
└─────────────────────────────────────────────┘
```

### AI 聊天視窗
```
┌─────────────────────────────────┐
│ Vibe AI Assistant           [×] │
│ Your personal shopping guide    │
├─────────────────────────────────┤
│                                 │
│     What are your best...  ◄─┐ │
│                               │ │
│ ┌─ We have several...        │ │
│ │   [打字效果游標]▮          │ │
│ └─                            │ │
│                               │ │
├─────────────────────────────────┤
│ [Ask me anything...     ] [→]  │
└─────────────────────────────────┘
```

---

## 🔍 測試 API 端點

### 1. 健康檢查
```bash
curl http://localhost:8787/health
```

**預期回應**:
```json
{
  "status": "healthy",
  "timestamp": "2026-01-13T...",
  "uptime": "edge-deployed"
}
```

### 2. 語義搜尋（無需登入）
```bash
curl "http://localhost:8787/api/search?query=wireless"
```

**預期回應**:
```json
{
  "success": true,
  "data": {
    "products": [...],
    "total": 1,
    "method": "hybrid"
  }
}
```

### 3. AI 聊天（需要 Clerk Token）
```bash
# 先從瀏覽器取得 token
# 然後執行:
curl -X POST http://localhost:8787/api/ai/chat \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{"message": "Hello!"}'
```

**預期回應**: SSE 串流

---

## 🐛 常見問題

### Q1: 向量化失敗 - "GEMINI_API_KEY required"
**解決方案**:
```bash
# 確保已設定環境變數
export $(cat .dev.vars | xargs)
echo $GEMINI_API_KEY  # 應該顯示你的 API Key
```

### Q2: 搜尋沒有結果
**解決方案**:
```bash
# 檢查商品是否已向量化
npx tsx scripts/check-products.ts

# 如果沒有，執行向量化
export $(cat .dev.vars | xargs)
npx tsx scripts/vectorize-products.ts
```

### Q3: AI 聊天無法連接
**解決方案**:
- 確認後端運行: http://localhost:8787/health
- 確認已登入 Clerk 帳號
- 查看瀏覽器 Console 錯誤訊息

### Q4: Rate Limit 錯誤
**解決方案**:
- 等待幾分鐘後重試
- AI 聊天: 20 次/15分鐘
- 搜尋: 60 次/分鐘

---

## 📚 更多資源

- **詳細使用指南**: `/docs/PHASE_3_USAGE.md`
- **完成報告**: `/docs/PHASE_3_COMPLETED.md`
- **技術規格**: `/docs/PHASE_3_SPEC.md`
- **腳本說明**: `/backend/scripts/README.md`

---

## 🎉 恭喜！

如果所有測試都通過，你已經成功設置並運行了 Phase 3 的所有 AI 功能！

接下來可以：
1. 探索更多自然語言查詢
2. 測試不同的對話場景
3. 查看推薦算法的效果
4. 準備部署到生產環境

---

**需要協助？** 查看文檔或檢查錯誤日誌：
- 後端日誌: 執行 `npm run dev` 的終端
- 前端日誌: 瀏覽器 DevTools Console
- API 錯誤: Network tab 中的請求詳情
